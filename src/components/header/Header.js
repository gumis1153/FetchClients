import React from "react";
import style from "../header/Sign.module.scss";

const Header = () => {
  return (
    <header>
      <h1 className={style.title}>Companies incomes</h1>
    </header>
  );
};

export default Header;
